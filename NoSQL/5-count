// 5-count.js

// Utiliser la base de données passée en argument (ex : mongo my_db)
use process.argv[2];

// Afficher le nombre de documents dans la collection school
print(db.school.countDocuments());
